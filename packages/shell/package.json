{
  "name": "@nex/shell",
  "version": "0.1.0",
  "private": true,
  "description": "Thin Electron shell for New Nex: loads the daemon-served UI, adds native chrome.",
  "main": "dist/main.js",
  "scripts": {
    "build": "node scripts/bundle.mjs",
    "start": "electron ."
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "esbuild": "^0.27.0"
  }
}
