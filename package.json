{
  "name": "kelpi",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "typecheck": "tsc -b packages/protocol packages/core packages/daemon packages/cli && tsc -p packages/client/tsconfig.json && tsc -p packages/shell/tsconfig.json",
    "test": "vitest run && pnpm --filter @kelpi/shell test",
    "check": "pnpm typecheck && pnpm test",
    "dist": "pnpm --filter @kelpi/daemon build && pnpm --filter @kelpi/client build && pnpm --filter @kelpi/cli build && pnpm --filter @kelpi/shell build && pnpm --filter @kelpi/shell make"
  },
  "devDependencies": {
    "@types/node": "^24",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild",
      "fs-xattr",
      "macos-alias",
      "node-pty"
    ],
    "overrides": {
      "ghostty-web": "file:vendor/ghostty-web-patched"
    }
  }
}
